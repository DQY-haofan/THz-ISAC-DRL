# config.yml - LEO-ISAC MARL Configuration

# Environment Configuration
environment:
  n_satellites: 4
  altitude_km: 550
  inclination_deg: 53.0
  frequency_ghz: 300
  bandwidth_ghz: 10
  tx_power_dbm: 30
  noise_figure_db: 3.0
  max_isl_range_km: 5000

# ISAC Task Configuration
isac:
  w_comm: 1.0          # Communication reward weight
  w_sens: 0.5          # Sensing reward weight
  w_penalty: 10.0      # Constraint violation penalty weight
  sensing_mode: "cooperative_orbit_determination"
  min_links_for_sensing: 3

# Agent Configuration
agent:
  actor_lr: 0.0001
  critic_lr: 0.001
  reward_lr: 0.001
  tau: 0.005           # Soft update rate
  gamma: 0.99          # Discount factor
  buffer_capacity: 1000000
  batch_size: 256
  use_prioritized_replay: true
  use_difference_rewards: true
  use_gru: false       # Default to MLP baseline

# Network Architecture
network:
  hidden_dims: [256, 128]
  gat_hidden: 64
  gat_heads: 4
  gru_hidden: 128
  dropout: 0.1

# Training Configuration
training:
  max_episodes: 1000
  max_steps_per_episode: 100
  warmup_episodes: 50
  update_frequency: 1
  noise_decay_rate: 0.995
  min_noise: 0.01
  gradient_clip_norm: 0.5

# Evaluation Configuration
evaluation:
  frequency: 50        # Evaluate every N episodes
  episodes: 10         # Episodes per evaluation
  benchmark_sca: true
  compare_baselines: true

# Logging Configuration
logging:
  use_tensorboard: true
  save_frequency: 100
  verbose: true